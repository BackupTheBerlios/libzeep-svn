#!/usr/bin/make -f

%:
	dh $@ --parallel

override_dh_auto_install:
	$(MAKE) LIBDIR=$(CURDIR)/debian/libzeep2/usr/lib install-libs
	$(MAKE) LIBDIR=$(CURDIR)/debian/libzeep-dev/usr/lib \
			INCDIR=$(CURDIR)/debian/libzeep-dev/usr/include \
			MANDIR=$(CURDIR)/debian/libzeep-dev/usr/share/man/man3 \
			install-dev
